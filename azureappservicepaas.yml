trigger:
- main

stages:
- stage: 'Thisisbuildstage'
  jobs:
  - job: Buildjob
    pool: Mynewpool
    steps:
    # - task: Npm@1
    #   inputs:
    #     command: 'install'
    - script: echo Hello, world!
      displayName: 'Run a one-line script'
    - task: Npm@1
      inputs:
        command: 'install'

